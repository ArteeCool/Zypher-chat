{
    "version": 2,
    "builds": [{
        "src": "./backend/server.ts",
        "use": "@vercel/node"}],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/backend/"
      }
    ],
    "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}